export enum EType {
  CLASSIC = "Classic",
  SERVER_SIDE = "Server-side",
  MVT = "MVT",
}

export enum EStatus {
  DRAFT = "Draft",
  ONLINE = "Online",
  PAUSED = "Paused",
  STOPPED = "Stopped",
}
